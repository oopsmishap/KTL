# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.10)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr and automatically regenerate CMakeLists.txt
	include("cmake/cmkr.cmake" OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
endif()

# Options
option(KTL_BUILD_TESTS "Build the KTL test driver." ON)

# Variables
set(CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
set(ENABLE_WDK_EXTENDED_CPP_FEATURES ON)
set(TARGET_WINVER _WIN32_WINNT_WIN7)
set(TARGET_NTDDI_VERSION NTDDI_WIN7)

include("cmake/msvc-configurations.cmake")

project(ktl
	LANGUAGES
		CXX
		ASM_MASM
)

# Packages
find_package(WDK REQUIRED)

# Target: ktl-runtime-x64
set(ktl-runtime-x64_SOURCES
	cmake.toml
	"ktl_runtime/src/exceptions/x64/capture.asm"
	"ktl_runtime/src/exceptions/x64/cookie.asm"
)

add_library(ktl-runtime-x64 STATIC)

target_sources(ktl-runtime-x64 PRIVATE ${ktl-runtime-x64_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${ktl-runtime-x64_SOURCES})

add_library(ktl::ktl-runtime::x64 ALIAS ktl-runtime-x64)
# Target: ktl-runtime
set(ktl-runtime_SOURCES
	cmake.toml
	"ktl_runtime/src/array_unwind.cpp"
	"ktl_runtime/src/assert.cpp"
	"ktl_runtime/src/basic_runtime.cpp"
	"ktl_runtime/src/bugcheck.cpp"
	"ktl_runtime/src/chrono.cpp"
	"ktl_runtime/src/cookie.cpp"
	"ktl_runtime/src/exception.cpp"
	"ktl_runtime/src/exceptions/gs_handler.cpp"
	"ktl_runtime/src/exceptions/member_ptr.cpp"
	"ktl_runtime/src/exceptions/terminate.cpp"
	"ktl_runtime/src/floating_point.cpp"
	"ktl_runtime/src/heap.cpp"
	"ktl_runtime/src/irql.cpp"
	"ktl_runtime/src/minifilter.cpp"
	"ktl_runtime/src/object_management.cpp"
	"ktl_runtime/src/placement_new.cpp"
	"ktl_runtime/src/preload_initializer.cpp"
	"ktl_runtime/src/type_info.cpp"
)

if(CMAKE_SIZEOF_VOID_P EQUAL 8) # x64
	list(APPEND ktl-runtime_SOURCES
		"ktl_runtime/src/exceptions/x64/catch.cpp"
		"ktl_runtime/src/exceptions/x64/frame_handler3.cpp"
		"ktl_runtime/src/exceptions/x64/frame_handler4.cpp"
		"ktl_runtime/src/exceptions/x64/throw.cpp"
		"ktl_runtime/src/exceptions/x64/unwind_handler.cpp"
	)
endif()

wdk_add_library(ktl-runtime)

target_sources(ktl-runtime PRIVATE ${ktl-runtime_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${ktl-runtime_SOURCES})

add_library(ktl::ktl-runtime ALIAS ktl-runtime)
target_compile_features(ktl-runtime PUBLIC
	cxx_std_20
)

target_compile_definitions(ktl-runtime PRIVATE
	_CRT_SECURE_CPP_OVERLOAD_SECURE_NAMES=0
	KTL_ENABLE_EXTENDED_ALIGNED_STORAGE
)

target_compile_options(ktl-runtime PRIVATE
	"/MP"
	"/O2"
	"/Oi"
	"/Ot"
	"/Oy"
	"/Gy"
	"/Gw"
	"/GS"
)

target_include_directories(ktl-runtime PUBLIC
	"ktl_runtime/include"
)

if(CMAKE_SIZEOF_VOID_P EQUAL 8) # x64
	target_link_libraries(ktl-runtime PUBLIC
		ktl::ktl-runtime::x64
	)
endif()

target_link_options(ktl-runtime PRIVATE
	"/OPT:REF"
	"/INCLUDE:verify_seh"
	"/INCLUDE:verify_seh_in_cxx_handler"
	"/OPT:ICF"
)

# Target: ktl
set(ktl_SOURCES
	cmake.toml
	"ktl/src/condition_variable.cpp"
	"ktl/src/ktlexcept.cpp"
	"ktl/src/literals.cpp"
	"ktl/src/mutex.cpp"
	"ktl/src/new_delete.cpp"
	"ktl/src/push_lock.cpp"
	"ktl/src/thread.cpp"
)

wdk_add_library(ktl)

target_sources(ktl PRIVATE ${ktl_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${ktl_SOURCES})

add_library(ktl::ktl ALIAS ktl)
target_compile_features(ktl PUBLIC
	cxx_std_20
)

target_compile_definitions(ktl PRIVATE
	FMT_EXCEPTIONS
	FMT_USE_NONTYPE_TEMPLATE_PARAMETERS=0
	_CRT_SECURE_CPP_OVERLOAD_SECURE_NAMES=0
)

target_compile_options(ktl PRIVATE
	"/MP"
	"/O2"
	"/Oi"
	"/Ot"
	"/Oy"
	"/Gy"
	"/Gw"
	"/GS"
)

target_include_directories(ktl PUBLIC
	"ktl/include"
)

target_link_libraries(ktl PUBLIC
	ktl::ktl-runtime
)

target_link_options(ktl PRIVATE
	"/OPT:REF"
	"/INCLUDE:verify_seh"
	"/INCLUDE:verify_seh_in_cxx_handler"
	"/OPT:ICF"
)

# Target: ktl-tests
if(KTL_BUILD_TESTS) # build-tests
	set(ktl-tests_SOURCES
		cmake.toml
		"ktl_tests/src/driver.cpp"
		"ktl_tests/src/dynamic_init.cpp"
		"ktl_tests/src/exception_dispatcher.cpp"
		"ktl_tests/src/floating_point.cpp"
		"ktl_tests/src/heap.cpp"
		"ktl_tests/src/irql.cpp"
		"ktl_tests/src/placement_new.cpp"
		"ktl_tests/src/preload_init.cpp"
		"ktl_tests/src/test_runner.cpp"
	)

	wdk_add_driver(ktl-tests)

	target_sources(ktl-tests PRIVATE ${ktl-tests_SOURCES})
	source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${ktl-tests_SOURCES})

	target_compile_features(ktl-tests PRIVATE
		cxx_std_20
	)

	target_compile_options(ktl-tests PRIVATE
		"/MP"
		"/O2"
		"/Oi"
		"/Ot"
		"/Oy"
		"/Gy"
		"/Gw"
		"/GS"
	)

	target_link_libraries(ktl-tests PRIVATE
		ktl::ktl
	)

	target_link_options(ktl-tests PRIVATE
		"/OPT:REF"
		"/INCLUDE:verify_seh"
		"/INCLUDE:verify_seh_in_cxx_handler"
		"/OPT:ICF"
	)

	target_include_directories(ktl-tests PRIVATE
		"ktl_tests/include"
	)

	get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
	if(NOT CMKR_VS_STARTUP_PROJECT)
		set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT ktl-tests)
	endif()

endif()
